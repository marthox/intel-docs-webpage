---
type CssClass = "banner-image" | "hero-image" | "event-image";

interface Props {
    key: string;
    data: {
        fields: {
            file: { url: string };
            description: string;
        };
    };
    css_class: CssClass;
}

const { key, data, css_class }: Props = Astro.props;

const {
    fields: {
        file: { url: image_url },
        description: image_alt,
    },
} = data;
---

<img class={css_class} src={`https://${image_url}`} alt={image_alt} />
<style>
    .banner-image {
        max-width: 40%;
        object-fit: cover;
    }
    .hero-image {
        max-width: 800px;
        width: 60%;
        height: auto;
        object-fit: cover;
    }
    .event-image {
        max-width: 200px;
        height: auto;
        object-fit: cover;
    }

    @media (max-width: 768px) {
        .banner-image {
            min-width: 100%;
        }
        .hero-image {
            min-width: 100%;
        }
        .event-image {
            min-width: 100%;
            margin-bottom: 1rem;
        }
    }
</style>
